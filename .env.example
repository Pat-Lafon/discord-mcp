# Required: Your Discord account credentials
DISCORD_EMAIL=your_email@example.com
DISCORD_PASSWORD=your_password

# Optional: false drives a visible browser, the one path that logs in with the
# credentials above and reseeds the cookie. Default true (cookie-only).
DISCORD_HEADLESS=true
